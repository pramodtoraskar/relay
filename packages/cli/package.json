{
  "name": "@relay/cli",
  "version": "0.1.0",
  "description": "Relay CLI - terminal interface for check-in, tasks, and handoffs",
  "main": "dist/index.js",
  "bin": {
    "relay": "dist/index.js"
  },
  "scripts": {
    "build": "npx tsup src/index.ts --format cjs --dts --clean",
    "dev": "tsup src/index.ts --format cjs --watch"
  },
  "dependencies": {
    "@relay/core": "*",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "dotenv": "^16.3.1",
    "inquirer": "^9.2.12",
    "ora": "^8.0.1",
    "nanoid": "^5.0.4"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
